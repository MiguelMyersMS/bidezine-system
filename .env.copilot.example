# bidezine-system — Copilot/Codex environment template
#
# Copy to `.env.copilot` on EACH machine and fill in values. NEVER commit `.env.copilot`
# (gitignored). Mirror the entries from `../design-system/.env.copilot` — same tooling/accounts.
