{
  "name": "origin-rail-sidebar-app",
  "version": "0.0.0",
  "description": "Quarantined, runnable copy of the origin RailNav. Built as its OWN Vite project so origin JS and CSS execute in their own bundle and their own browsing context, never the Sandbox app's. Never imported by any bidezine app — see docs/SANDBOX-SPEC.md invariant 6.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "dev": "vite --port 4198 --strictPort"
  },
  "dependencies": {
    "@radix-ui/react-dropdown-menu": "^2.1.15",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
